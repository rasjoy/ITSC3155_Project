<h1>Character Page</h1>
<p>Below is a list of all created characters.</p>
<table>
    <tr>
        <th><h2>Name</h2</th>
        <th><h2>Job</h2></th>
        <th><h2>Race</h2></th>
        <th><h2>Birthdate</h2></th>
    </tr>
    
    
    <% @characters.each do |character| %>
    <tr>
        <td> <%= link_to character.name, character_path(character) %></td>
        <td><%= character.job.name if character.job %></td>
        <td></td>
        <td><%= character.created_at %></td>
    </tr>
    <% end %>
</table>